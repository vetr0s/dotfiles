general {
        colors = false
        interval = 5
}

order += "disk /"
order += "load"
order += "tztime local"

disk "/" {
        format = "disk %avail"
}

load {
        format = "load %1min"
}

tztime local {
        format = "%a %d %b  %H:%M"
}
